name: clock_sessions
description: "A Flutter app to track office time and calculate monthly earnings"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  path_provider: ^2.1.5
  flutter_local_notifications: ^19.4.2
  drift: ^2.28.2
  sqlite3_flutter_libs: ^0.5.40
  path: ^1.9.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.28.3
  build_runner: ^2.9.0

flutter:
  uses-material-design: true
